# Local MCP Servers – Setup Guide

This power requires the Jira and Sumo MCP servers to be installed locally.

---

## Step 1: Install MCP Servers

Clone and install the MCP servers repository:

```bash
git clone ssh://git@oxfordssh.awsdev.infor.com:7999/EdwinReginoJr/local-mcp-servers.git
cd local-mcp-servers
./install.sh
